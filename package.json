{
  "name": "taho-shop",
  "private": true,
  "scripts": {
    "predev": "node scripts/free-ports.js 3000 8080",
    "dev": "npm run db:up --prefix shop-be && concurrently -n BE,FE -c blue,green \"npm run dev --prefix shop-be\" \"npm run dev --prefix shop-fe\"",
    "db:up": "npm run db:up --prefix shop-be",
    "db:down": "npm run db:down --prefix shop-be",
    "db:status": "npm run db:status --prefix shop-be"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
